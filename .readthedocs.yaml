version: 2

build:
  os: ubuntu-22.04

  apt_packages:
    - doxygen
    - graphviz

  commands:
    - git submodule update --init --recursive
    - doxygen docs/Doxyfile
conda:
  environment: docs/environment.yml